# Guia rápido

1. Copie os arquivos para seu repositório.
2. Execute os 3 arquivos SQL na ordem.
3. Rode `pip install -r requirements.txt`.
4. Rode `python python/analysis.py`.
5. Faça `git add .`, `git commit -m "feat: adiciona projeto completo de análise de vendas"` e `git push origin main`.
